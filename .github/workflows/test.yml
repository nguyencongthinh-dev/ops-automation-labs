name: Closed-Loop Orchestrator CI/CD

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  lint:
    name: Code Quality & Security Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install uv and tools
        uses: astral-sh/setup-uv@v5
        with:
          version: 'latest'

      - name: Run Ruff Linter
        run: |
          uv pip install ruff
          uv run ruff check lab-closed-loop/tdd

      - name: Run Ruff Formatter Check
        run: |
          uv run ruff format --check lab-closed-loop/tdd

      - name: Run Security Scan (Bandit)
        run: |
          uv pip install bandit
          uv run bandit -r lab-closed-loop/tdd/ -x lab-closed-loop/tdd/test_closed_loop.py

      - name: Run ShellCheck on Runbooks
        uses: koalaman/shellcheck-action@v2
        with:
          path: lab-closed-loop/tdd/runbooks

  test:
    name: Unit Tests & Coverage
    needs: lint
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ['3.11']
    defaults:
      run:
        working-directory: lab-closed-loop

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          version: 'latest'
          enable-cache: true

      - name: Install dependencies
        run: |
          uv pip install --system -r requirements.txt pytest pytest-cov

      - name: Run tests with coverage
        run: |
          python -m pytest tdd/test_closed_loop.py --cov=tdd --cov-report=term-missing
